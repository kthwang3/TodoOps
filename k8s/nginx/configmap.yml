apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: development
data:
  nginx.conf: |
    server {
      listen 80;

      location / {
        proxy_pass http://webapp-service:3000;
      }
    }