apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: development
spec:
  storageClassName: ""
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi