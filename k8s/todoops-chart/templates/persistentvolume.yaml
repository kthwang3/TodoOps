apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-pv
spec:
  storageClassName: ""
  capacity:
    storage: {{ .Values.mongo.storageSize }}
  accessModes: 
    - ReadWriteOnce
  hostPath: 
    path: /mnt/data/mongo

